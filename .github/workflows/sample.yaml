name: "Sample Workflow"
on: 
    push: 
        branches: 
        - master
jobs: 
    testing_job:
        runs-on: ubuntu-latest
        steps: 
            - name: "Welcome Step"
              run: |
                echo "Welcome to this step"